interface accessibleString {
  label: string;
  hint?: string;
}

export const SearchScreenAccessibleStrings = {
  searchScreen: {
    label: 'Search Source',
    hint: 'Tap to search for Pack',
  } as accessibleString,
};